/**
 * add - adds two numbers and returns their sum
 * @a: the first number to be added
 * @b: the second numbers to be added
 * Return: the sum of @a and @b
 */
int add(int a, int b)
{
return (a + b);
}
