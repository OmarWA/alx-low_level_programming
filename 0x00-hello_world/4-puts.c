#include<stdio.h>

/**
 * main - the entry point for the program
 * Return: a status code (0 if everything went well)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
