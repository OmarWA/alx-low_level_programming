/**
 * mul - a funciton that evaluates and returns the product of two numbers
 * @a: a number to be multiplies with @b
 * @b: another number to be multiplied with @a
 * Return: the product of @a and @b
 */
int mul(int a, int b)
{
return (a * b);
}
